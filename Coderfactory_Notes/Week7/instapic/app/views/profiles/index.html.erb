<h1>Profile Index</h1>
<% @profiles.each do |profile| %>
  <p>
    <%= profile.first_name %>
    <%= profile.last_name %>
    <%= profile.phone %>
    <%= link_to "Show", user_profile_path(@user, profile) %>
  </p>
<% end %>

<%= link_to "New Profile", new_user_profile_path %>
